usr/include/*
usr/lib/lib*.so usr/lib/@DEB_HOST_MULTIARCH@
usr/lib/lib*.a usr/lib/@DEB_HOST_MULTIARCH@
docs/samples usr/share/doc/libpugixml-dev/examples
#the ./ in front of the CMAKE_ROOT is needed because CMAKE_ROOT is absolute,
#but the path here has to be relative
usr/*.cmake ./@CMAKE_ROOT@/Modules
